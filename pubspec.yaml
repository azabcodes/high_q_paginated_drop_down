name: high_q_paginated_drop_down
description: Package handles the pagination and search in DropDown .
version: 2.1.3
homepage: https://github.com/mazab99/high_q_paginated_drop_down

environment:
  sdk: ^3.8.0


dependencies:
  flutter:
    sdk: flutter
  async: ^2.13.0

dev_dependencies:
  recase: ^4.1.0
  flutter_test:
    sdk: flutter
  version: ^2.0.0
  flutter_lints: ^5.0.0
